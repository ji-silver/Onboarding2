import React from 'react';


export interface IBoardTableProps {
    //children?: React.ReactNode,
}

const BoardTable = ({}: IBoardTableProps) => {
    return (
        <div>
        </div>
    );
};


BoardTable.defaultProps = {};

export default BoardTable;